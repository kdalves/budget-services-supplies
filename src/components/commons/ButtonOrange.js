import React, { Component } from 'react';
import { Button} from '@material-ui/core';

class ButtonOrange extends Component {
  render() {    
    return (
      <Button text="Ver Orçamento" className="button-orange"/>
    );
  }
}

export default ButtonOrange;
